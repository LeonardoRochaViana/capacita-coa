"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { PageHeader } from "@/components/layout/PageHeader";
import { Workflow, ArrowRight, CircleDot, Flag } from "lucide-react";
import type { AccessibleResourcePayload, Fluxograma } from "@/types";
export default function Fluxos(){const [items,setItems]=useState<Fluxograma[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{fetch("/api/fluxogramas").then(async response=>{const payload=await response.json();if(!response.ok)throw new Error(payload.error);setItems((payload as AccessibleResourcePayload<Fluxograma>).items);}).catch(reason=>setError(reason instanceof Error?reason.message:"Não foi possível carregar os fluxogramas.")).finally(()=>setLoading(false));},[]);if(loading)return <div className="card p-8 text-center text-sm text-gray-500">Carregando fluxogramas autorizados...</div>;if(error)return <div className="card p-8 text-center text-sm text-red-700">{error}</div>;return <><PageHeader eyebrow="Caminho operacional" title="Fluxogramas" description="Veja de onde o processo parte, quais decisões atravessa e como deve terminar."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{items.map(f=><Link href={`/fluxogramas/${f.id}`} key={f.id} className="card group p-5 transition hover:-translate-y-0.5 hover:border-leaf/40"><div className="flex items-center justify-between"><span className="grid h-11 w-11 place-items-center rounded-xl bg-red-50 text-leaf"><Workflow size={20}/></span><ArrowRight size={17} className="text-gray-300 group-hover:text-leaf"/></div><p className="mt-5 text-[10px] font-bold uppercase tracking-wider text-leaf">{f.mesa}</p><h2 className="mt-2 min-h-10 text-sm font-bold">{f.nome}</h2><p className="mt-2 text-xs leading-5 text-gray-500">{f.objetivo}</p><div className="mt-5 grid grid-cols-2 gap-2 border-t border-line pt-4 text-[11px]"><span className="flex items-center gap-1 text-gray-500"><CircleDot size={12} className="text-slate-600"/> {f.entrada}</span><span className="flex items-center gap-1 text-gray-500"><Flag size={12} className="text-leaf"/> {f.saida}</span></div></Link>)}</div></>;}
